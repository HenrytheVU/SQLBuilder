package elements;

import java.util.List;

public class ForBusinessTimeAsOfCurrentDate extends AbstractSQLElement {
    public ForBusinessTimeAsOfCurrentDate(StringBuilder query, List<Object> params) {
        super(query, params);
    }
}
